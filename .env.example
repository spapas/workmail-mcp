# Copy to .env only if your local launcher explicitly loads dotenv files.
# Never commit a populated .env file.

WORKMAIL_IMAP_HOST=imap.example.invalid
WORKMAIL_IMAP_PORT=993
WORKMAIL_IMAP_USERNAME=user@example.invalid
WORKMAIL_IMAP_PASSWORD=replace-me
WORKMAIL_HTTP_ADDR=127.0.0.1:8787
WORKMAIL_API_TOKEN=replace-with-a-random-256-bit-token
