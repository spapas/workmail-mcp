# workmail-mcp

A local, read-only bridge between an IMAP mailbox (initially Zimbra) and MCP-capable AI clients.

The project is being bootstrapped. The first implementation target is Windows, with Hermes using MCP over stdio and ChatGPT using a localhost MCP HTTP transport through the supported ChatGPT connection path.
